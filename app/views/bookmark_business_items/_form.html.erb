<%= form_for(@bookmark_business_item) do |f| %>
  <% if @bookmark_business_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bookmark_business_item.errors.count, "error") %> prohibited this bookmark_business_item from being saved:</h2>

      <ul>
      <% @bookmark_business_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :business_item_id %><br />
    <%= f.number_field :business_item_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
