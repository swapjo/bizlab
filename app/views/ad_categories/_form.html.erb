<%= form_for(@ad_category) do |f| %>
    <% if @ad_category.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@ad_category.errors.count, "error") %> prohibited this ad_category from being saved:</h2>

          <ul>
            <% @ad_category.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :name %>
      <br/>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :introtext %>
      <br/>
      <%= f.text_field :introtext %>
    </div>
    <div class="field">
      <%= f.label :description %>
      <br/>
      <%= f.text_area :description %>
    </div>
    <div class="field">
      <%= f.label :image %>
      <br/>
      <%= f.file_field :image %>
    </div>
    <div class="field">
      <%= f.label :ispublished %>
      <br/>
      <%= f.check_box :ispublished %>
    </div>


    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
