<%= form_for(@bookmark_job) do |f| %>
  <% if @bookmark_job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bookmark_job.errors.count, "error") %> prohibited this bookmark_job from being saved:</h2>

      <ul>
      <% @bookmark_job.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :job_id %><br />
    <%= f.number_field :job_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
