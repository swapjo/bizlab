<%= form_for(@mall_type) do |f| %>
    <% if @mall_type.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@mall_type.errors.count, "error") %> prohibited this mall_type from being saved:</h2>

          <ul>
            <% @mall_type.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :name %>
      <br/>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :introtext %>
      <br/>
      <%= f.text_field :introtext %>
    </div>
    <div class="field">
      <%= f.label :description %>
      <br/>
      <%= f.text_area :description %>
    </div>
    <div class="field">
      <%= f.label :ispublished %>
      <br/>
      <%= f.check_box :ispublished %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
